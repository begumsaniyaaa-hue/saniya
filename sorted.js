function isSorted(arr){
    let n=arr.length;
    for(let i=0;i<n-1;i++){
        
        return false;
    
    
}
return false;
}

let arr1=[1,2,3,4,5]

let arr2=[1,2,3,5,4]

console.log(isSorted(arr1)); 

console.log(isSorted(arr2));
